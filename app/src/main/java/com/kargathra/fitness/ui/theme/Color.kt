package com.kargathra.fitness.ui.theme

import androidx.compose.ui.graphics.Color

// --- Kargathra palette -------------------------------------------------
// Navy carries the surfaces; gold is reserved for emphasis and progress so
// it stays meaningful. Body text is near-white for maximum legibility on
// dark surfaces; gold is never used for long-form text.

val NavyDeep      = Color(0xFF0A1626) // app background
val NavySurface   = Color(0xFF11233B) // cards / sheets
val NavyElevated  = Color(0xFF1B3354) // raised cards, selected states

val NavyLine      = Color(0xFF294466) // hairlines / dividers
val Gold          = Color(0xFFC9A341) // primary accent
val GoldBright    = Color(0xFFE0BE5C) // hover / pressed accent
val GoldDim       = Color(0xFF8C7330) // disabled accent

val OffWhite      = Color(0xFFF2F4F8) // primary text
val MutedText     = Color(0xFFAEBACF) // secondary text
val FaintText     = Color(0xFF7C8AA3) // tertiary / hints

val SuccessGreen  = Color(0xFF5BBF8A)
val WarnAmber     = Color(0xFFD9A441)
val DangerRed     = Color(0xFFD96A5B)
